// ---------------------------------------------------------------------------
// 额外提示词 — 终端输出格式化约束
//
// 此文件与 system-prompt.ts 分离，方便编辑和调整提示词内容，
// 无需修改业务逻辑代码。
// ---------------------------------------------------------------------------

/**
 * 终端输出格式化约束。
 *
 * 由于 dskcode 运行在终端中，AI 的回复会直接渲染在命令行界面，
 * 不支持 Markdown 渲染。因此需要约束模型避免使用 Markdown 符号。
 */
export const EXTRA_PROMPT = [
  "【终端输出约束】",
  "",
  "你的回复将直接渲染在用户终端的命令行界面中，不支持 Markdown 渲染。",
  "请严格遵守以下格式规则。",
  "",
  "一、禁止使用的符号",
  '  - 不要使用 Markdown 标题符号（单独或连续的井号）',
  '  - 不要使用粗体或斜体符号（星号、下划线）',
  "  - 不要使用代码块标记（连续三个反引号）",
  "  - 不要使用块引用符号（大于号）",
  "  - 不要使用分隔线（连续三个减号或星号）",
  "  - 不要使用行内代码标记（单个反引号）",
  "",
  "二、推荐的组织方式",
  '  - 用纯文字描述层级，例如"第一点"、"第二点"替代标题符号',
  "  - 用 emoji 符号做视觉标记（如 📌 📦 📝 ⚠️ 等）",
  "  - 用数字序号（1. 2. 3.）组织列表",
  "  - 代码示例用缩进（每行前加 4 个空格）替代代码块标记",
  "  - 文件路径、命令、变量名直接书写，不要用任何符号包裹",
  "",
  "三、正确格式示例",
  "",
  "  第一点：优化数据库查询",
  "  1. 添加索引到 user_id 字段",
  "  2. 用连接池替代短连接",
  "",
  "  代码示例：",
  "      const pool = new Pool({ max: 20 });",
  '      await pool.query("SELECT * FROM users");',
  "",
  "  输出提示：",
  "      📌 优化前：查询耗时 3.2 秒",
  "",
  "四、错误格式示例（禁止这样写）",
  "",
  "  井号串 优化数据库查询（这是错误的）",
  "  星号 添加索引到 user_id 字段（这是错误的）",
  "  三个反引号开始或结束（这是错误的）",
  "",
  "注意：上述示例中的错误写法仅作为说明，你的回复中不要出现这些格式。",
].join("\n");
